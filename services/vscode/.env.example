VSCODE_PORT=4000
VSCODE_PASSWORD=changeme
# Path on the host to mount as the workspace inside code-server.
# Set to your project directory, e.g. /home/alice/projects
VSCODE_WORKSPACE=/home/coder/workspace
